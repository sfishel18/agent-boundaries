{
  "name": "agent-boundaries",
  "private": true,
  "scripts": {
    "install:all": "cd opencode && bun install",
    "test": "cd opencode && bun run test",
    "test:watch": "cd opencode && bun run test:watch"
  }
}
